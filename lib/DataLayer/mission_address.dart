class MissionAddress{
  // Holds the address of the currently selected mission. Used so detail type
  // widgets know which mission to look up. Currently using the Firestore document ID.
  String address;
}