import 'package:flutter/material.dart';
import 'package:mission_out/UI/signin_screen.dart';

class MainScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    if (true){
      return SigninScreen();
    }
  }

}